# AgendaBackEnd

This is a node.js express api that i will use for my agenda

To run on server: `npm run start`

To run locally (no mySQL db access, doesn't work anymore, DO NOT USE): `npm run start local`

Reads database information from .env file
